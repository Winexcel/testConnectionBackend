const userConfig = {
  login: 'eltex',
  password: 'eltexpassword',
  accessToken: 'eltex',
};

const testConfig = {
  maxValue: 0,
  currentValue: 0,
  result: 0,
  isCompleted: true,
}

module.exports = {
  userConfig,
  testConfig,
};
